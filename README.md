# WP to FastAPI - Day 1
Migrated PHP thinking to FastAPI + Postgres in Docker

## Stack
FastAPI, SQLAlchemy, Postgres 15, Docker Compose, uv

## Run
docker compose up --build
-> http://localhost:8000/docs

## Endpoints
GET /posts, GET /posts/{id}, POST /posts
